package it.pissir.breakout.game;

/** Tipus de power-up que pot deixar anar un bloc en ser destruït. */
public enum PowerUpType {
    /** Afegeix una segona pilota a la partida. */
    EXTRA_BALL,
    /** Dona x2 de punts durant 10 segons. */
    DOUBLE_SCORE
}
